[**frontend**](../../../README.md)

***

[frontend](../../../modules.md) / [composables/LocationFallback](../README.md) / createMap

# Function: createMap()

> **createMap**(`element`, `location`): `Map`

Defined in: src/composables/LocationFallback.ts:48

Creates a Leaflet map instance centered at the given location.

## Parameters

### element

`HTMLElement`

The HTML element to mount the map on.

### location

`MaybeRefOrGetter`\<`LatLng`\>

The initial center location for the map (can be a ref or getter).

## Returns

`Map`

The Leaflet map instance.
